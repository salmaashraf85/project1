// src/pages/Saved.tsx
import { Box } from '@mui/material';
import { FC } from 'react';
import React from 'react';

const Saved: FC = () => {
  return (
  <Box>Saved</Box>
  )
};

export default Saved;